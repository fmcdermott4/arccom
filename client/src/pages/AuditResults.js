import React, {useState} from 'react';
import {useQuery} from '@apollo/client';

const AuditResults = () => {

  
  return (
    <main>
      Hello Audit Results!
    </main>
  );
};

export default AuditResults;